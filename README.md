# trimble-extension
trimble shuffler
this extension allow the user to create layers from private properties
first export a csv of your datatable with the colums you want 
2nd reimport this csv in trimble using the button import
select any of your columns to create layers that you can switch on or off
